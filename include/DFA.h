/**
 * @brief Client-facing header file for DFA
 */
#ifndef _DFA_h
#define _DFA_h

#define MAX_STATES 100
#define MAX_TRANSITIONS 50

// Deterministic Finite Automata have the following characteristics
// - For any state s, and any input x, there is _at most_ one transition
// out of the state s whose label includes x.

#endif //_DFA_h
